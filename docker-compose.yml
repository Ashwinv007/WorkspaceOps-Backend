services:
  backend:
    image: ashwin0705/workspaceops-backend:latest
    container_name: workspaceops-backend
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data:
