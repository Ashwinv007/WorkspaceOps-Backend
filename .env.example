# ─────────────────────────────────────────────
# WorkspaceOps Backend — Environment Variables
# Copy this file to .env and fill in the values
# ─────────────────────────────────────────────

# Server
PORT=4000

# ── Database ─────────────────────────────────
# Option 1: Local MongoDB (development)
MONGO_URI=mongodb://localhost:27017/workspaceops

# Option 2: MongoDB Atlas (cloud — recommended for team dev & production)
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/workspaceops?retryWrites=true&w=majority

# ── Auth ─────────────────────────────────────
# Change this to a long random string in production (min 32 chars)
JWT_SECRET=your_jwt_secret_here_change_this_in_production

# ── File Upload ───────────────────────────────
# Local storage (development). Switch to S3 in production.
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10

# ── Business Logic ────────────────────────────
# Documents expiring within this many days are marked as EXPIRING
EXPIRY_WARNING_DAYS=30

# ── CORS ─────────────────────────────────────
# Frontend origin. In production, set to your actual frontend URL.
# Example: FRONTEND_URL=https://app.yourcompany.com
# Leave unset or use * to allow all origins (development only)
FRONTEND_URL=http://localhost:3000
